[package]
name = "taxid_locator"
version = "0.1.0"
edition = "2021"

[dependencies]
bio = "1"
regex = "1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.0", features = ["derive"] }
